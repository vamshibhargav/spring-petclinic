pipeline {
  agent none
  tools { 
        maven 'Maven'
        jdk 'JAVA_HOME'
  }
  stages {
    agent { label "NewNode" }
    stage('Clone repository') {
      steps {
        checkout scm
      }
    }
    stage('Build') {
      steps {
        sh 'mvn clean package'
        sh 'echo $USER'
        sh 'echo whoami'
      }
    }
 }
}


pipeline {
  agent any
  stages {
   stage('Clone repository') {
      steps {
        checkout scm
        echo "Sudhakar"
      }
    }
    stage('Build'){
      steps {
        sh 'mvn compile'
        echo "compilation done"
      }
    }
  }
}
